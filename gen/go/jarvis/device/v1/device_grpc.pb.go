// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: jarvis/device/v1/device.proto

package devicev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	DeviceService_GetCapabilities_FullMethodName = "/jarvis.device.v1.DeviceService/GetCapabilities"
	DeviceService_ExecuteCommand_FullMethodName  = "/jarvis.device.v1.DeviceService/ExecuteCommand"
)

// DeviceServiceClient is the client API for DeviceService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// DeviceService runs on a user's computer (the Mac local daemon) and executes
// commands for the orchestrator inside an operating-system sandbox.
//
// Security contract:
//   - Reachable only over the user's Tailscale tailnet, with mutual TLS. The
//     caller's identity is the URI SAN of its client certificate.
//   - No shell. `program` (an absolute path) is executed directly with `args`
//     as its argument vector.
//   - Commands in the device's allowlist run immediately. Any other command
//     runs only after one of the user's approver devices signs an approval
//     for exactly that command (see ApprovalRequired). The daemon verifies
//     the signature itself, so a compromised orchestrator cannot approve on
//     the user's behalf. Programs on the device's deny list never run.
//   - Every command runs sandboxed: no network, no writes and no access to
//     macOS services unless granted (see SandboxGrants); credential stores
//     such as ~/.ssh, keychains and .env files are never readable.
//
// Errors use canonical gRPC status codes. When a caller may need to branch on
// the cause, the status carries a google.rpc.ErrorInfo detail with domain
// "device.jarvis" and `reason` set to an ErrorReason value name.
type DeviceServiceClient interface {
	// GetCapabilities describes the device and the policy it enforces, so the
	// orchestrator can plan commands that will run without approval.
	//
	// Errors:
	//   PERMISSION_DENIED  caller may not call this method.
	GetCapabilities(ctx context.Context, in *GetCapabilitiesRequest, opts ...grpc.CallOption) (*GetCapabilitiesResponse, error)
	// ExecuteCommand runs one command, or answers with ApprovalRequired when
	// the command needs the user's signed approval first. To proceed, call
	// again with the same command and `approval` set.
	//
	// Errors:
	//   INVALID_ARGUMENT     a field is missing or malformed.
	//   PERMISSION_DENIED    ERROR_REASON_COMMAND_DENIED,
	//                        ERROR_REASON_WORKING_DIRECTORY_NOT_ALLOWED,
	//                        ERROR_REASON_APPROVAL_INVALID, or the caller may not
	//                        call this method.
	//   FAILED_PRECONDITION  ERROR_REASON_APPROVAL_EXPIRED (unknown, used or
	//                        expired approval id) or ERROR_REASON_NO_APPROVERS.
	//   RESOURCE_EXHAUSTED   ERROR_REASON_TOO_MANY_COMMANDS or
	//                        ERROR_REASON_TOO_MANY_PENDING_APPROVALS.
	ExecuteCommand(ctx context.Context, in *ExecuteCommandRequest, opts ...grpc.CallOption) (*ExecuteCommandResponse, error)
}

type deviceServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewDeviceServiceClient(cc grpc.ClientConnInterface) DeviceServiceClient {
	return &deviceServiceClient{cc}
}

func (c *deviceServiceClient) GetCapabilities(ctx context.Context, in *GetCapabilitiesRequest, opts ...grpc.CallOption) (*GetCapabilitiesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetCapabilitiesResponse)
	err := c.cc.Invoke(ctx, DeviceService_GetCapabilities_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *deviceServiceClient) ExecuteCommand(ctx context.Context, in *ExecuteCommandRequest, opts ...grpc.CallOption) (*ExecuteCommandResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ExecuteCommandResponse)
	err := c.cc.Invoke(ctx, DeviceService_ExecuteCommand_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// DeviceServiceServer is the server API for DeviceService service.
// All implementations must embed UnimplementedDeviceServiceServer
// for forward compatibility.
//
// DeviceService runs on a user's computer (the Mac local daemon) and executes
// commands for the orchestrator inside an operating-system sandbox.
//
// Security contract:
//   - Reachable only over the user's Tailscale tailnet, with mutual TLS. The
//     caller's identity is the URI SAN of its client certificate.
//   - No shell. `program` (an absolute path) is executed directly with `args`
//     as its argument vector.
//   - Commands in the device's allowlist run immediately. Any other command
//     runs only after one of the user's approver devices signs an approval
//     for exactly that command (see ApprovalRequired). The daemon verifies
//     the signature itself, so a compromised orchestrator cannot approve on
//     the user's behalf. Programs on the device's deny list never run.
//   - Every command runs sandboxed: no network, no writes and no access to
//     macOS services unless granted (see SandboxGrants); credential stores
//     such as ~/.ssh, keychains and .env files are never readable.
//
// Errors use canonical gRPC status codes. When a caller may need to branch on
// the cause, the status carries a google.rpc.ErrorInfo detail with domain
// "device.jarvis" and `reason` set to an ErrorReason value name.
type DeviceServiceServer interface {
	// GetCapabilities describes the device and the policy it enforces, so the
	// orchestrator can plan commands that will run without approval.
	//
	// Errors:
	//   PERMISSION_DENIED  caller may not call this method.
	GetCapabilities(context.Context, *GetCapabilitiesRequest) (*GetCapabilitiesResponse, error)
	// ExecuteCommand runs one command, or answers with ApprovalRequired when
	// the command needs the user's signed approval first. To proceed, call
	// again with the same command and `approval` set.
	//
	// Errors:
	//   INVALID_ARGUMENT     a field is missing or malformed.
	//   PERMISSION_DENIED    ERROR_REASON_COMMAND_DENIED,
	//                        ERROR_REASON_WORKING_DIRECTORY_NOT_ALLOWED,
	//                        ERROR_REASON_APPROVAL_INVALID, or the caller may not
	//                        call this method.
	//   FAILED_PRECONDITION  ERROR_REASON_APPROVAL_EXPIRED (unknown, used or
	//                        expired approval id) or ERROR_REASON_NO_APPROVERS.
	//   RESOURCE_EXHAUSTED   ERROR_REASON_TOO_MANY_COMMANDS or
	//                        ERROR_REASON_TOO_MANY_PENDING_APPROVALS.
	ExecuteCommand(context.Context, *ExecuteCommandRequest) (*ExecuteCommandResponse, error)
	mustEmbedUnimplementedDeviceServiceServer()
}

// UnimplementedDeviceServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedDeviceServiceServer struct{}

func (UnimplementedDeviceServiceServer) GetCapabilities(context.Context, *GetCapabilitiesRequest) (*GetCapabilitiesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetCapabilities not implemented")
}
func (UnimplementedDeviceServiceServer) ExecuteCommand(context.Context, *ExecuteCommandRequest) (*ExecuteCommandResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ExecuteCommand not implemented")
}
func (UnimplementedDeviceServiceServer) mustEmbedUnimplementedDeviceServiceServer() {}
func (UnimplementedDeviceServiceServer) testEmbeddedByValue()                       {}

// UnsafeDeviceServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to DeviceServiceServer will
// result in compilation errors.
type UnsafeDeviceServiceServer interface {
	mustEmbedUnimplementedDeviceServiceServer()
}

func RegisterDeviceServiceServer(s grpc.ServiceRegistrar, srv DeviceServiceServer) {
	// If the following call panics, it indicates UnimplementedDeviceServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&DeviceService_ServiceDesc, srv)
}

func _DeviceService_GetCapabilities_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetCapabilitiesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DeviceServiceServer).GetCapabilities(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DeviceService_GetCapabilities_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DeviceServiceServer).GetCapabilities(ctx, req.(*GetCapabilitiesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _DeviceService_ExecuteCommand_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ExecuteCommandRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(DeviceServiceServer).ExecuteCommand(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: DeviceService_ExecuteCommand_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(DeviceServiceServer).ExecuteCommand(ctx, req.(*ExecuteCommandRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// DeviceService_ServiceDesc is the grpc.ServiceDesc for DeviceService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var DeviceService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "jarvis.device.v1.DeviceService",
	HandlerType: (*DeviceServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetCapabilities",
			Handler:    _DeviceService_GetCapabilities_Handler,
		},
		{
			MethodName: "ExecuteCommand",
			Handler:    _DeviceService_ExecuteCommand_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "jarvis/device/v1/device.proto",
}
