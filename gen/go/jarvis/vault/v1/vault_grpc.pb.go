// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: jarvis/vault/v1/vault.proto

package vaultv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	VaultService_CreateKey_FullMethodName       = "/jarvis.vault.v1.VaultService/CreateKey"
	VaultService_GetDecryptedKey_FullMethodName = "/jarvis.vault.v1.VaultService/GetDecryptedKey"
	VaultService_RevokeKey_FullMethodName       = "/jarvis.vault.v1.VaultService/RevokeKey"
	VaultService_SealData_FullMethodName        = "/jarvis.vault.v1.VaultService/SealData"
	VaultService_OpenData_FullMethodName        = "/jarvis.vault.v1.VaultService/OpenData"
)

// VaultServiceClient is the client API for VaultService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// VaultService stores tenant-owned ("bring your own key") provider API keys
// encrypted at rest and releases plaintext only to authorised internal
// services.
//
// Security contract, binding on every implementation and caller:
//   - Plaintext secrets exist only in process memory, are zeroized after use,
//     and are never written to disk, logs, traces, metrics or error messages.
//   - Every field carrying plaintext is marked `debug_redact`. Logging and
//     tracing interceptors in every language MUST honour that option.
//   - Callers authenticate with mutual TLS. Caller identity comes from the
//     client certificate, never from request fields.
//   - `tenant_id` in a request is checked against the authenticated principal.
//     A key that exists but belongs to another tenant is reported as
//     NOT_FOUND, so key existence cannot be probed across tenants.
//   - Callers send an `x-request-id` metadata header; it is recorded in the
//     audit log alongside the caller identity and key id.
//
// Errors use canonical gRPC status codes. When a caller may need to branch on
// the cause, the status carries a google.rpc.ErrorInfo detail with domain
// "vault.jarvis" and `reason` set to an ErrorReason value name.
type VaultServiceClient interface {
	// CreateKey encrypts and stores a new provider API key for a tenant.
	//
	// A tenant has at most one ACTIVE key per provider. Set `replace_active` to
	// rotate without a gap in which no key is available.
	//
	// Idempotent on `request_id`: a retry with the same id and payload returns
	// the original response.
	//
	// Errors:
	//   INVALID_ARGUMENT   a field is missing or malformed.
	//   ALREADY_EXISTS     ERROR_REASON_ACTIVE_KEY_EXISTS, or
	//                      ERROR_REASON_REQUEST_ID_CONFLICT (same request_id,
	//                      different payload).
	//   PERMISSION_DENIED  caller may not manage keys for this tenant.
	CreateKey(ctx context.Context, in *CreateKeyRequest, opts ...grpc.CallOption) (*CreateKeyResponse, error)
	// GetDecryptedKey returns the plaintext of an ACTIVE key.
	//
	// Restricted to internal runtime services (voice gateway, orchestrator, MCP
	// router). It must never be reachable from the dashboard or a client app.
	// Every call, successful or not, is audit-logged without the secret.
	//
	// Errors:
	//   INVALID_ARGUMENT     a field is missing or malformed.
	//   NOT_FOUND            ERROR_REASON_KEY_NOT_FOUND: no such key for this
	//                        tenant, or no ACTIVE key for the given provider.
	//   FAILED_PRECONDITION  ERROR_REASON_KEY_REVOKED: the key was revoked.
	//   PERMISSION_DENIED    caller may not read plaintext keys.
	GetDecryptedKey(ctx context.Context, in *GetDecryptedKeyRequest, opts ...grpc.CallOption) (*GetDecryptedKeyResponse, error)
	// RevokeKey permanently disables a key and destroys its ciphertext
	// (crypto-shredding). Metadata is kept for the audit trail. Irreversible.
	//
	// Idempotent: revoking an already revoked key succeeds and returns the
	// existing metadata unchanged.
	//
	// Errors:
	//   INVALID_ARGUMENT   a field is missing or malformed.
	//   NOT_FOUND          ERROR_REASON_KEY_NOT_FOUND.
	//   PERMISSION_DENIED  caller may not manage keys for this tenant.
	RevokeKey(ctx context.Context, in *RevokeKeyRequest, opts ...grpc.CallOption) (*RevokeKeyResponse, error)
	// SealData encrypts a small secret that the calling service stores itself
	// (for example OAuth tokens), bound to a tenant, a purpose and a subject.
	// The same envelope encryption as provider keys; only the Vault can open it.
	//
	// Errors:
	//   INVALID_ARGUMENT   a field is missing or malformed.
	//   PERMISSION_DENIED  caller may not seal data.
	SealData(ctx context.Context, in *SealDataRequest, opts ...grpc.CallOption) (*SealDataResponse, error)
	// OpenData decrypts what SealData produced. Tenant, purpose and subject
	// must be exactly those it was sealed with.
	//
	// Errors:
	//   INVALID_ARGUMENT     a field is missing or malformed.
	//   FAILED_PRECONDITION  ERROR_REASON_SEALED_DATA_INVALID: different
	//                        binding, tampered, or sealed under another master key.
	//   PERMISSION_DENIED    caller may not open data.
	OpenData(ctx context.Context, in *OpenDataRequest, opts ...grpc.CallOption) (*OpenDataResponse, error)
}

type vaultServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewVaultServiceClient(cc grpc.ClientConnInterface) VaultServiceClient {
	return &vaultServiceClient{cc}
}

func (c *vaultServiceClient) CreateKey(ctx context.Context, in *CreateKeyRequest, opts ...grpc.CallOption) (*CreateKeyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CreateKeyResponse)
	err := c.cc.Invoke(ctx, VaultService_CreateKey_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *vaultServiceClient) GetDecryptedKey(ctx context.Context, in *GetDecryptedKeyRequest, opts ...grpc.CallOption) (*GetDecryptedKeyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetDecryptedKeyResponse)
	err := c.cc.Invoke(ctx, VaultService_GetDecryptedKey_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *vaultServiceClient) RevokeKey(ctx context.Context, in *RevokeKeyRequest, opts ...grpc.CallOption) (*RevokeKeyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RevokeKeyResponse)
	err := c.cc.Invoke(ctx, VaultService_RevokeKey_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *vaultServiceClient) SealData(ctx context.Context, in *SealDataRequest, opts ...grpc.CallOption) (*SealDataResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SealDataResponse)
	err := c.cc.Invoke(ctx, VaultService_SealData_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *vaultServiceClient) OpenData(ctx context.Context, in *OpenDataRequest, opts ...grpc.CallOption) (*OpenDataResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(OpenDataResponse)
	err := c.cc.Invoke(ctx, VaultService_OpenData_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// VaultServiceServer is the server API for VaultService service.
// All implementations must embed UnimplementedVaultServiceServer
// for forward compatibility.
//
// VaultService stores tenant-owned ("bring your own key") provider API keys
// encrypted at rest and releases plaintext only to authorised internal
// services.
//
// Security contract, binding on every implementation and caller:
//   - Plaintext secrets exist only in process memory, are zeroized after use,
//     and are never written to disk, logs, traces, metrics or error messages.
//   - Every field carrying plaintext is marked `debug_redact`. Logging and
//     tracing interceptors in every language MUST honour that option.
//   - Callers authenticate with mutual TLS. Caller identity comes from the
//     client certificate, never from request fields.
//   - `tenant_id` in a request is checked against the authenticated principal.
//     A key that exists but belongs to another tenant is reported as
//     NOT_FOUND, so key existence cannot be probed across tenants.
//   - Callers send an `x-request-id` metadata header; it is recorded in the
//     audit log alongside the caller identity and key id.
//
// Errors use canonical gRPC status codes. When a caller may need to branch on
// the cause, the status carries a google.rpc.ErrorInfo detail with domain
// "vault.jarvis" and `reason` set to an ErrorReason value name.
type VaultServiceServer interface {
	// CreateKey encrypts and stores a new provider API key for a tenant.
	//
	// A tenant has at most one ACTIVE key per provider. Set `replace_active` to
	// rotate without a gap in which no key is available.
	//
	// Idempotent on `request_id`: a retry with the same id and payload returns
	// the original response.
	//
	// Errors:
	//   INVALID_ARGUMENT   a field is missing or malformed.
	//   ALREADY_EXISTS     ERROR_REASON_ACTIVE_KEY_EXISTS, or
	//                      ERROR_REASON_REQUEST_ID_CONFLICT (same request_id,
	//                      different payload).
	//   PERMISSION_DENIED  caller may not manage keys for this tenant.
	CreateKey(context.Context, *CreateKeyRequest) (*CreateKeyResponse, error)
	// GetDecryptedKey returns the plaintext of an ACTIVE key.
	//
	// Restricted to internal runtime services (voice gateway, orchestrator, MCP
	// router). It must never be reachable from the dashboard or a client app.
	// Every call, successful or not, is audit-logged without the secret.
	//
	// Errors:
	//   INVALID_ARGUMENT     a field is missing or malformed.
	//   NOT_FOUND            ERROR_REASON_KEY_NOT_FOUND: no such key for this
	//                        tenant, or no ACTIVE key for the given provider.
	//   FAILED_PRECONDITION  ERROR_REASON_KEY_REVOKED: the key was revoked.
	//   PERMISSION_DENIED    caller may not read plaintext keys.
	GetDecryptedKey(context.Context, *GetDecryptedKeyRequest) (*GetDecryptedKeyResponse, error)
	// RevokeKey permanently disables a key and destroys its ciphertext
	// (crypto-shredding). Metadata is kept for the audit trail. Irreversible.
	//
	// Idempotent: revoking an already revoked key succeeds and returns the
	// existing metadata unchanged.
	//
	// Errors:
	//   INVALID_ARGUMENT   a field is missing or malformed.
	//   NOT_FOUND          ERROR_REASON_KEY_NOT_FOUND.
	//   PERMISSION_DENIED  caller may not manage keys for this tenant.
	RevokeKey(context.Context, *RevokeKeyRequest) (*RevokeKeyResponse, error)
	// SealData encrypts a small secret that the calling service stores itself
	// (for example OAuth tokens), bound to a tenant, a purpose and a subject.
	// The same envelope encryption as provider keys; only the Vault can open it.
	//
	// Errors:
	//   INVALID_ARGUMENT   a field is missing or malformed.
	//   PERMISSION_DENIED  caller may not seal data.
	SealData(context.Context, *SealDataRequest) (*SealDataResponse, error)
	// OpenData decrypts what SealData produced. Tenant, purpose and subject
	// must be exactly those it was sealed with.
	//
	// Errors:
	//   INVALID_ARGUMENT     a field is missing or malformed.
	//   FAILED_PRECONDITION  ERROR_REASON_SEALED_DATA_INVALID: different
	//                        binding, tampered, or sealed under another master key.
	//   PERMISSION_DENIED    caller may not open data.
	OpenData(context.Context, *OpenDataRequest) (*OpenDataResponse, error)
	mustEmbedUnimplementedVaultServiceServer()
}

// UnimplementedVaultServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedVaultServiceServer struct{}

func (UnimplementedVaultServiceServer) CreateKey(context.Context, *CreateKeyRequest) (*CreateKeyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CreateKey not implemented")
}
func (UnimplementedVaultServiceServer) GetDecryptedKey(context.Context, *GetDecryptedKeyRequest) (*GetDecryptedKeyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetDecryptedKey not implemented")
}
func (UnimplementedVaultServiceServer) RevokeKey(context.Context, *RevokeKeyRequest) (*RevokeKeyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RevokeKey not implemented")
}
func (UnimplementedVaultServiceServer) SealData(context.Context, *SealDataRequest) (*SealDataResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method SealData not implemented")
}
func (UnimplementedVaultServiceServer) OpenData(context.Context, *OpenDataRequest) (*OpenDataResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method OpenData not implemented")
}
func (UnimplementedVaultServiceServer) mustEmbedUnimplementedVaultServiceServer() {}
func (UnimplementedVaultServiceServer) testEmbeddedByValue()                      {}

// UnsafeVaultServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to VaultServiceServer will
// result in compilation errors.
type UnsafeVaultServiceServer interface {
	mustEmbedUnimplementedVaultServiceServer()
}

func RegisterVaultServiceServer(s grpc.ServiceRegistrar, srv VaultServiceServer) {
	// If the following call panics, it indicates UnimplementedVaultServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&VaultService_ServiceDesc, srv)
}

func _VaultService_CreateKey_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateKeyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(VaultServiceServer).CreateKey(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: VaultService_CreateKey_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(VaultServiceServer).CreateKey(ctx, req.(*CreateKeyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _VaultService_GetDecryptedKey_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetDecryptedKeyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(VaultServiceServer).GetDecryptedKey(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: VaultService_GetDecryptedKey_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(VaultServiceServer).GetDecryptedKey(ctx, req.(*GetDecryptedKeyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _VaultService_RevokeKey_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeKeyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(VaultServiceServer).RevokeKey(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: VaultService_RevokeKey_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(VaultServiceServer).RevokeKey(ctx, req.(*RevokeKeyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _VaultService_SealData_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SealDataRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(VaultServiceServer).SealData(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: VaultService_SealData_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(VaultServiceServer).SealData(ctx, req.(*SealDataRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _VaultService_OpenData_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(OpenDataRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(VaultServiceServer).OpenData(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: VaultService_OpenData_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(VaultServiceServer).OpenData(ctx, req.(*OpenDataRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// VaultService_ServiceDesc is the grpc.ServiceDesc for VaultService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var VaultService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "jarvis.vault.v1.VaultService",
	HandlerType: (*VaultServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "CreateKey",
			Handler:    _VaultService_CreateKey_Handler,
		},
		{
			MethodName: "GetDecryptedKey",
			Handler:    _VaultService_GetDecryptedKey_Handler,
		},
		{
			MethodName: "RevokeKey",
			Handler:    _VaultService_RevokeKey_Handler,
		},
		{
			MethodName: "SealData",
			Handler:    _VaultService_SealData_Handler,
		},
		{
			MethodName: "OpenData",
			Handler:    _VaultService_OpenData_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "jarvis/vault/v1/vault.proto",
}
