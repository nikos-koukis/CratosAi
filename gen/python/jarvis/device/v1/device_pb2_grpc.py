# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from jarvis.device.v1 import device_pb2 as jarvis_dot_device_dot_v1_dot_device__pb2

GRPC_GENERATED_VERSION = '1.84.0'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in jarvis/device/v1/device_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class DeviceServiceStub:
    """DeviceService runs on a user's computer (the Mac local daemon) and executes
    commands for the orchestrator inside an operating-system sandbox.

    Security contract:
    * Reachable only over the user's Tailscale tailnet, with mutual TLS. The
    caller's identity is the URI SAN of its client certificate.
    * No shell. `program` (an absolute path) is executed directly with `args`
    as its argument vector.
    * Commands in the device's allowlist run immediately. Any other command
    runs only after one of the user's approver devices signs an approval
    for exactly that command (see ApprovalRequired). The daemon verifies
    the signature itself, so a compromised orchestrator cannot approve on
    the user's behalf. Programs on the device's deny list never run.
    * Every command runs sandboxed: no network, no writes and no access to
    macOS services unless granted (see SandboxGrants); credential stores
    such as ~/.ssh, keychains and .env files are never readable.

    Errors use canonical gRPC status codes. When a caller may need to branch on
    the cause, the status carries a google.rpc.ErrorInfo detail with domain
    "device.jarvis" and `reason` set to an ErrorReason value name.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.GetCapabilities = channel.unary_unary(
                '/jarvis.device.v1.DeviceService/GetCapabilities',
                request_serializer=jarvis_dot_device_dot_v1_dot_device__pb2.GetCapabilitiesRequest.SerializeToString,
                response_deserializer=jarvis_dot_device_dot_v1_dot_device__pb2.GetCapabilitiesResponse.FromString,
                _registered_method=True)
        self.ExecuteCommand = channel.unary_unary(
                '/jarvis.device.v1.DeviceService/ExecuteCommand',
                request_serializer=jarvis_dot_device_dot_v1_dot_device__pb2.ExecuteCommandRequest.SerializeToString,
                response_deserializer=jarvis_dot_device_dot_v1_dot_device__pb2.ExecuteCommandResponse.FromString,
                _registered_method=True)


class DeviceServiceServicer:
    """DeviceService runs on a user's computer (the Mac local daemon) and executes
    commands for the orchestrator inside an operating-system sandbox.

    Security contract:
    * Reachable only over the user's Tailscale tailnet, with mutual TLS. The
    caller's identity is the URI SAN of its client certificate.
    * No shell. `program` (an absolute path) is executed directly with `args`
    as its argument vector.
    * Commands in the device's allowlist run immediately. Any other command
    runs only after one of the user's approver devices signs an approval
    for exactly that command (see ApprovalRequired). The daemon verifies
    the signature itself, so a compromised orchestrator cannot approve on
    the user's behalf. Programs on the device's deny list never run.
    * Every command runs sandboxed: no network, no writes and no access to
    macOS services unless granted (see SandboxGrants); credential stores
    such as ~/.ssh, keychains and .env files are never readable.

    Errors use canonical gRPC status codes. When a caller may need to branch on
    the cause, the status carries a google.rpc.ErrorInfo detail with domain
    "device.jarvis" and `reason` set to an ErrorReason value name.
    """

    def GetCapabilities(self, request, context):
        """GetCapabilities describes the device and the policy it enforces, so the
        orchestrator can plan commands that will run without approval.

        Errors:
        PERMISSION_DENIED  caller may not call this method.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def ExecuteCommand(self, request, context):
        """ExecuteCommand runs one command, or answers with ApprovalRequired when
        the command needs the user's signed approval first. To proceed, call
        again with the same command and `approval` set.

        Errors:
        INVALID_ARGUMENT     a field is missing or malformed.
        PERMISSION_DENIED    ERROR_REASON_COMMAND_DENIED,
        ERROR_REASON_WORKING_DIRECTORY_NOT_ALLOWED,
        ERROR_REASON_APPROVAL_INVALID, or the caller may not
        call this method.
        FAILED_PRECONDITION  ERROR_REASON_APPROVAL_EXPIRED (unknown, used or
        expired approval id) or ERROR_REASON_NO_APPROVERS.
        RESOURCE_EXHAUSTED   ERROR_REASON_TOO_MANY_COMMANDS or
        ERROR_REASON_TOO_MANY_PENDING_APPROVALS.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_DeviceServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'GetCapabilities': grpc.unary_unary_rpc_method_handler(
                    servicer.GetCapabilities,
                    request_deserializer=jarvis_dot_device_dot_v1_dot_device__pb2.GetCapabilitiesRequest.FromString,
                    response_serializer=jarvis_dot_device_dot_v1_dot_device__pb2.GetCapabilitiesResponse.SerializeToString,
            ),
            'ExecuteCommand': grpc.unary_unary_rpc_method_handler(
                    servicer.ExecuteCommand,
                    request_deserializer=jarvis_dot_device_dot_v1_dot_device__pb2.ExecuteCommandRequest.FromString,
                    response_serializer=jarvis_dot_device_dot_v1_dot_device__pb2.ExecuteCommandResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'jarvis.device.v1.DeviceService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('jarvis.device.v1.DeviceService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class DeviceService:
    """DeviceService runs on a user's computer (the Mac local daemon) and executes
    commands for the orchestrator inside an operating-system sandbox.

    Security contract:
    * Reachable only over the user's Tailscale tailnet, with mutual TLS. The
    caller's identity is the URI SAN of its client certificate.
    * No shell. `program` (an absolute path) is executed directly with `args`
    as its argument vector.
    * Commands in the device's allowlist run immediately. Any other command
    runs only after one of the user's approver devices signs an approval
    for exactly that command (see ApprovalRequired). The daemon verifies
    the signature itself, so a compromised orchestrator cannot approve on
    the user's behalf. Programs on the device's deny list never run.
    * Every command runs sandboxed: no network, no writes and no access to
    macOS services unless granted (see SandboxGrants); credential stores
    such as ~/.ssh, keychains and .env files are never readable.

    Errors use canonical gRPC status codes. When a caller may need to branch on
    the cause, the status carries a google.rpc.ErrorInfo detail with domain
    "device.jarvis" and `reason` set to an ErrorReason value name.
    """

    @staticmethod
    def GetCapabilities(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/jarvis.device.v1.DeviceService/GetCapabilities',
            jarvis_dot_device_dot_v1_dot_device__pb2.GetCapabilitiesRequest.SerializeToString,
            jarvis_dot_device_dot_v1_dot_device__pb2.GetCapabilitiesResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def ExecuteCommand(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/jarvis.device.v1.DeviceService/ExecuteCommand',
            jarvis_dot_device_dot_v1_dot_device__pb2.ExecuteCommandRequest.SerializeToString,
            jarvis_dot_device_dot_v1_dot_device__pb2.ExecuteCommandResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
