{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "app-api",
  "projectType": "application",
  "sourceRoot": "services/app-api",
  "tags": ["type:service", "lang:go"],
  "implicitDependencies": ["proto", "go-libs"],
  "namedInputs": {
    "go": [
      "{projectRoot}/**/*",
      "!{projectRoot}/.dev/**/*",
      "{workspaceRoot}/gen/go/**/*",
      "{workspaceRoot}/libs/go/**/*",
      "{workspaceRoot}/go.work",
      "{workspaceRoot}/tools/go/go.mod"
    ]
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["go"],
      "options": { "command": "go build ./...", "cwd": "services/app-api" }
    },
    "test": {
      "executor": "nx:run-commands",
      "inputs": ["go"],
      "options": { "command": "go test -race ./...", "cwd": "services/app-api" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["go"],
      "options": {
        "command": "go vet ./... && go tool staticcheck ./... && bash -n scripts/*.sh",
        "cwd": "services/app-api"
      }
    },
    "format-check": {
      "executor": "nx:run-commands",
      "inputs": ["go"],
      "options": {
        "command": "test -z \"$(gofmt -l .)\" || { gofmt -l .; exit 1; }",
        "cwd": "services/app-api"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": { "command": "gofmt -w .", "cwd": "services/app-api" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": { "command": "scripts/dev-run.sh", "cwd": "services/app-api" }
    }
  }
}
