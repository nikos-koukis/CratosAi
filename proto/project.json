{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "proto",
  "projectType": "library",
  "sourceRoot": "proto",
  "tags": [
    "type:contract"
  ],
  "namedInputs": {
    "protos": [
      "{projectRoot}/**/*.proto",
      "{workspaceRoot}/buf.yaml"
    ]
  },
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "inputs": [
        "protos",
        "{workspaceRoot}/gen/go/**/*",
        "{workspaceRoot}/buf.gen.yaml",
        "{workspaceRoot}/tools/go/go.mod",
        "{workspaceRoot}/gen/python/**/*",
        "{workspaceRoot}/gen/python/pyproject.toml",
        "{workspaceRoot}/uv.lock",
        "{workspaceRoot}/buf.gen.connect.yaml",
        "{workspaceRoot}/gen/swift/Sources/**/*",
        "{workspaceRoot}/buf.gen.swift.yaml",
        "{workspaceRoot}/tools/swift/Package.swift",
        "{workspaceRoot}/gen/ts/src/**/*",
        "{workspaceRoot}/buf.gen.es.yaml"
      ],
      "options": {
        "command": "buf lint && tools/check-generated-go.sh && tools/check-generated-python.sh && tools/check-generated-swift.sh && tools/check-generated-ts.sh"
      }
    },
    "format-check": {
      "executor": "nx:run-commands",
      "inputs": [
        "protos"
      ],
      "options": {
        "command": "buf format --diff --exit-code"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buf format --write"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "inputs": [
        "protos"
      ],
      "options": {
        "command": "buf build"
      }
    },
    "breaking": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buf breaking --against '.git#branch=main'"
      }
    },
    "generate": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "rm -rf gen/go/jarvis && tools/gen-go.sh && (cd gen/go && go mod tidy) && tools/gen-python.sh && tools/gen-swift.sh && tools/gen-ts.sh"
      }
    }
  }
}
